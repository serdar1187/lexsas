.superpowers/
.worktrees/
docs/cv-source.md
_ziptmp/
*.bak
Thumbs.db
.DS_Store

# Nested private repo (brand/profile materials). Never committed here: this repo is
# published verbatim to lexsas.com by GitHub Pages, so any committed file becomes a public URL.
lexsas-tools/

# local concept/work folders
Lexsas Kimi/
_shots/
_verify*.py
linkedin/

# Full older copy of the site with identical canonical URLs. Kept local only: committing it
# would publish a duplicate-content shadow of every page on lexsas.com.
.pre-restore-backup/

# Internal working files. They stay on disk and in the private tools repo, but
# they must not be published: everything committed here becomes a public URL,
# and the design spec names employers in a way the public pages deliberately do not.
CLAUDE.md
docs/
content/
assets/brand/README.txt

# Claude Code workspace config (local only)
.claude/
